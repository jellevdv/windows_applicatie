<Page
    x:Class="WindowsApplicatie.Views.CategoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WindowsApplicatie.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer>
        <ListBox x:Name="CategoryList" Margin="5" Width="1502" Height="994" HorizontalAlignment="Left" ItemsSource="{Binding}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="2">
                        <TextBlock Text="{Binding Name}" Margin="2"/>
                        <TextBlock Text="(" Margin="10,2,0,2"/>
                        <TextBlock Text="{Binding Description}" Margin="2"/>
                        <TextBlock Text=")" Margin="10,2,0,2"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
            </ListBox>

    </ScrollViewer>
</Page>
