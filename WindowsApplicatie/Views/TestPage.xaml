<Page
    x:Class="WindowsApplicatie.Views.TestPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WindowsApplicatie.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:models="using:WindowsApplicatie.Models"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Grid>
    <ScrollViewer Grid.ColumnSpan="5">
        <ListBox x:Name="listViewHolidays" Margin="5" Width="1502" Height="659" ItemsSource="{Binding Items}" SelectedItem="SelectedItem" >
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="2">
                        <TextBlock Text="{Binding Name}" Margin="2"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>


           

    </ScrollViewer>


        <StackPanel x:Name="HolidayDetails" >
            <ListBox  ItemsSource="{Binding SelectedItem}" >
                <TextBlock FontWeight="Bold" Text="{Binding Name}"/>
                <TextBlock FontWeight="Bold" Text="hello" />
            </ListBox>
         
    </StackPanel>
    </Grid>

</Page>
