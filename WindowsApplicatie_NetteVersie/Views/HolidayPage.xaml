<Page
    x:Class="WindowsApplicatie_NetteVersie.Views.HolidayPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WindowsApplicatie_NetteVersie.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:WindowsApplicatie_NetteVersie.ViewModels"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.DataContext>
        <vm:HolidayListViewModel/>
    </Page.DataContext>

    <Page.Content>
        <StackPanel>
            <ListView x:Name="lvHolidays" ItemsSource="{Binding Holidays}" SelectedItem="{Binding SelectedHoliday}" Margin="72,72,72,77" Background="#FFEEEEEE">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="2">
                            <TextBlock Text="{Binding Name}" Margin="2"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Button x:Name="AddHolidayBtn" Content="Add Holiday" HorizontalAlignment="Right" Margin="0,0,72,0" MinWidth="148" Padding="4,4,4,4"/>
            <Button x:Name="RemoveHolidayBtn" Content="Remove Holiday" HorizontalAlignment="Right" Margin="0,24,72,0" MinWidth="148"/>
        </StackPanel>
    </Page.Content>

</Page>
